{{ form_start(form) }}
    <div class="row">
        <div class="col-md-4">
            {{ form_widget(form) }}
        </div>
    </div>

    <div class="mt-3">

        <button class="btn btn-lg btn-primary">{{ button_label|default('Enregistrer') }}</button>

        <a href="{{ path('admin.facility.list') }}"
            class="btn btn-lg btn-secondary">Annuler</a>
    </div>
{{ form_end(form) }}
